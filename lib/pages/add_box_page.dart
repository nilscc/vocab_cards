import 'package:flutter/material.dart';

class AddBoxPage extends StatelessWidget {
  const AddBoxPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Add box..."),),
      body: const Text("Moep"),
    );
  }
}